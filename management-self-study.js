(() => {
  const KEY='amlManagementSelfStudyV1';
  const PENDING_KEY='amlManagementSelfStudyPendingArticle';
  const IDS=['detective','direction','strategy','leadership','quality','systems'];
  const empty=()=>({articles:{},games:{},reflections:{},quiz:{passed:false,score:0,date:null,certificateId:null,name:null}});
  function load(){try{const v=JSON.parse(localStorage.getItem(KEY)||'null');return v&&typeof v==='object'?{...empty(),...v,articles:{...v.articles},games:{...v.games},reflections:{...v.reflections},quiz:{...empty().quiz,...v.quiz}}:empty();}catch(e){return empty();}}
  function save(s){localStorage.setItem(KEY,JSON.stringify(s));}
  function count(s){let n=0;IDS.forEach(id=>{if(s.articles[id])n++;if(s.games[id])n++;});return n;}
  function bonusCount(s){return IDS.filter(id=>((s.reflections[id]||'').trim().length>=30)).length;}
  function render(){const s=load();IDS.forEach(id=>{const card=document.querySelector(`[data-lesson="${id}"]`);if(!card)return;const a=!!s.articles[id],g=!!s.games[id];card.classList.toggle('is-done',a&&g);const as=card.querySelector('[data-article-status]'),gs=card.querySelector('[data-game-status]');if(as){as.textContent=a?'✓ 文章已讀':'○ 文章未標記';as.classList.toggle('ok',a);}if(gs){gs.textContent=g?'✓ 挑戰已完成':'○ 挑戰未完成';gs.classList.toggle('ok',g);}const btn=card.querySelector('[data-mark-read]');if(btn)btn.textContent=a?'取消文章已讀':'標記文章已讀';const ta=card.querySelector(`[data-reflection="${id}"]`);if(ta&&document.activeElement!==ta)ta.value=s.reflections[id]||'';updateReflectionUI(id,ta?ta.value:(s.reflections[id]||''));});const n=count(s),pct=Math.round(n/12*100);document.querySelector('[data-progress-count]').textContent=`${n} / 12`;document.querySelector('[data-progress-bar]').style.width=`${pct}%`;document.querySelector('[data-bonus-count]').textContent=`Reflection Bonus ${bonusCount(s)} / 6`;const quiz=document.querySelector('[data-quiz-link]');const unlocked=n===12;quiz.setAttribute('aria-disabled',unlocked?'false':'true');quiz.textContent=unlocked?'開始 Management Challenge →':'尚未解鎖';}
  function updateReflectionUI(id,value){const n=(value||'').trim().length;const c=document.querySelector(`[data-reflection-count="${id}"]`),st=document.querySelector(`[data-reflection-status="${id}"]`);if(c)c.textContent=`${n} / 100`;if(st){st.textContent=n>=30?'✓ Bonus 已完成':(n>0?'至少 30 字才計入 Bonus':'');st.className=n>=30?'bonus-ok':'';}}
  document.querySelectorAll('[data-article-link]').forEach(link=>link.addEventListener('click',()=>{const id=link.dataset.articleLink;if(id)sessionStorage.setItem(PENDING_KEY,id);}));
  function completePending(){const id=sessionStorage.getItem(PENDING_KEY);if(!id||!IDS.includes(id))return;sessionStorage.removeItem(PENDING_KEY);const s=load();s.articles[id]=true;save(s);render();const card=document.getElementById(`lesson-${id}`);if(card)setTimeout(()=>card.scrollIntoView({behavior:'smooth',block:'center'}),80);}
  document.querySelectorAll('[data-mark-read]').forEach(btn=>btn.addEventListener('click',()=>{const id=btn.dataset.markRead,s=load();s.articles[id]=!s.articles[id];save(s);render();}));
  document.querySelectorAll('[data-reflection]').forEach(ta=>ta.addEventListener('input',()=>updateReflectionUI(ta.dataset.reflection,ta.value)));
  document.querySelectorAll('[data-save-reflection]').forEach(btn=>btn.addEventListener('click',()=>{const id=btn.dataset.saveReflection,ta=document.querySelector(`[data-reflection="${id}"]`),text=(ta.value||'').trim();if(text.length<30){alert('加分反思至少需要 30 字。');ta.focus();return;}const s=load();s.reflections[id]=text;save(s);render();}));
  const reset=document.querySelector('[data-reset-progress]');if(reset)reset.addEventListener('click',()=>{if(!confirm('確定要清除這台裝置上的管理學自學紀錄嗎？\n\n文章、漫畫挑戰、Reflection Bonus、主測驗與完訓證明都會歸零。'))return;localStorage.removeItem(KEY);sessionStorage.removeItem(PENDING_KEY);render();window.scrollTo({top:0,behavior:'smooth'});});
  window.addEventListener('storage',e=>{if(e.key===KEY)render();});window.addEventListener('pageshow',()=>{render();completePending();});render();
})();
